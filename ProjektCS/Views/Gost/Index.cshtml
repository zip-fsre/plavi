@model IEnumerable<ProjektCS.Models.Gost>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_DOGADJAJA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IME_I_PREZIME)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BROJ_STOLA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.STATUS_GOSTA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.STATUS_DOLASKA)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_DOGADJAJA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IME_I_PREZIME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BROJ_STOLA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.STATUS_GOSTA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.STATUS_DOLASKA)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
