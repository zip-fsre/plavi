@model IEnumerable<ProjektCS.Models.Prostor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LOKACIJA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BROJ_STOLOVA)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BROJ_MJESTA_PO_STOLU)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UKUPAN_KAPACITET)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_ARANZMANA)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LOKACIJA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BROJ_STOLOVA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BROJ_MJESTA_PO_STOLU)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UKUPAN_KAPACITET)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_ARANZMANA)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
